name: Enterprise DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:

  build:

    name: BUILD STAGE

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print Message
        run: echo "Main Branch Pipeline Triggered"